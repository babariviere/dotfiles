(clpm-client:activate-context "default" :activate-asdf-integration t)

#-asdf (require :asdf)
#-cffi (asdf:load-system :cffi)

;; (if (member "--no-linedit" sb-ext:*posix-argv* :test 'equal)
;;     (setf sb-ext:*posix-argv* 
;; 	  (remove "--no-linedit" sb-ext:*posix-argv* :test 'equal))
;;     (when (interactive-stream-p *terminal-io*)
;;       (asdf:load-system :sb-aclrepl)
;;       (asdf:load-system :linedit)
;;       (funcall (intern "INSTALL-REPL" :linedit) :wrap-current t :eof-quits t)))
